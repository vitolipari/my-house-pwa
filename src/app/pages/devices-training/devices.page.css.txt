:host {
    display: block;
    min-height: 100vh;
    color: var(--color-text-1);
    background:
        radial-gradient(circle at 85% 5%, rgb(33 90 197 / 18%), transparent 32rem),
        var(--color-background-1);
}

.devices-page {
    width: min(1120px, calc(100% - 2rem));
    margin: 0 auto;
    padding: 2rem 0 5rem;
}

.hero,
.section-heading,
.device-title,
.discovered-list article {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
}

h1,
h2,
h3,
p {
    margin-top: 0;
}

h1 {
    margin-bottom: .45rem;
    font-size: clamp(2.15rem, 6vw, 4rem);
    line-height: 1;
}

h2 {
    margin-bottom: .25rem;
    font-size: 1.35rem;
}

h3 {
    margin: 0;
    font-size: 1rem;
}

.eyebrow {
    margin-bottom: .35rem;
    color: var(--sky-color);
    font-size: .72rem;
    font-weight: 600;
    letter-spacing: .16em;
    text-transform: uppercase;
}

.intro,
.help,
.device-main > p,
.integration span:last-child,
.discovered-list span {
    color: var(--medium-gray);
}

.hero {
    margin-bottom: 1.5rem;
}

button,
input,
select {
    box-sizing: border-box;
    border: 1px solid rgb(136 136 136 / 35%);
    border-radius: .75rem;
    font: inherit;
}

button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: .5rem;
    min-height: 2.8rem;
    padding: .65rem 1rem;
    color: #fff;
    background: linear-gradient(135deg, var(--sky-color), var(--dark-sky-color));
    cursor: pointer;
}

button.secondary {
    color: var(--color-text-1);
    background: var(--color-background-3);
}

button:disabled {
    cursor: wait;
    opacity: .55;
}

.panel,
.integration {
    border: 1px solid rgb(136 136 136 / 22%);
    background: color-mix(in srgb, var(--color-background-3) 92%, transparent);
    box-shadow: 0 1.25rem 3rem rgb(0 0 0 / 8%);
    backdrop-filter: blur(14px);
}

.panel {
    margin-bottom: 1rem;
    padding: clamp(1rem, 3vw, 1.6rem);
    border-radius: 1.25rem;
}

.integration-strip {
    display: flex;
    gap: .65rem;
    margin-bottom: 1rem;
    overflow-x: auto;
}

.integration {
    display: grid;
    grid-template-columns: auto auto;
    gap: .1rem .5rem;
    min-width: 12rem;
    padding: .75rem 1rem;
    border-radius: .9rem;
}

.integration span:last-child {
    grid-column: 2;
    font-size: .72rem;
}

.status-dot {
    align-self: center;
    width: .55rem;
    height: .55rem;
    border-radius: 50%;
    background: var(--medium-red-color);
}

.integration.ready .status-dot {
    background: var(--good-green);
    box-shadow: 0 0 .7rem var(--good-green);
}

.counter,
.badge {
    border-radius: 999px;
    font-size: .72rem;
    font-weight: 600;
    text-transform: uppercase;
}

.counter {
    min-width: 2rem;
    padding: .35rem .55rem;
    text-align: center;
    color: #fff;
    background: var(--dark-sky-color);
}

.device-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: .8rem;
    margin-top: 1rem;
}

.device-card {
    display: flex;
    gap: .9rem;
    padding: 1rem;
    border: 1px solid rgb(136 136 136 / 20%);
    border-radius: 1rem;
}

.device-icon {
    display: grid;
    flex: 0 0 2.8rem;
    width: 2.8rem;
    height: 2.8rem;
    place-items: center;
    border-radius: .85rem;
    color: var(--sky-color);
    background: rgb(33 150 243 / 12%);
}

.device-icon i {
    font-size: 1.2rem !important;
    color: inherit;
}

.device-main {
    min-width: 0;
    flex: 1;
}

.device-main > p {
    margin: .3rem 0 .75rem;
    font-size: .8rem;
}

.badge {
    padding: .25rem .45rem;
    color: var(--medium-red-color);
    background: var(--red-opacity);
}

.badge.online {
    color: var(--darkGreen);
    background: var(--very-light-gray-green);
}

dl,
dl div {
    margin: 0;
}

dl div {
    display: flex;
    justify-content: space-between;
    gap: .75rem;
    padding: .2rem 0;
    font-size: .75rem;
}

dt {
    color: var(--medium-gray);
}

dd {
    margin: 0;
    overflow: hidden;
    text-align: right;
    text-overflow: ellipsis;
}

.empty {
    padding: 2rem;
    text-align: center;
    color: var(--medium-gray);
}

.management-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
}

.form-grid {
    display: grid;
    gap: .8rem;
    margin-top: 1rem;
}

label {
    display: grid;
    gap: .35rem;
    color: var(--medium-gray);
    font-size: .78rem;
}

input,
select {
    width: 100%;
    min-height: 2.75rem;
    padding: .65rem .8rem;
    color: var(--color-text-1);
    background: var(--color-background-1);
}

.discovered-list {
    display: grid;
    gap: .5rem;
    margin-top: 1rem;
}

.discovered-list article {
    padding: .8rem;
    border: 1px solid rgb(136 136 136 / 18%);
    border-radius: .75rem;
}

.discovered-list article > div {
    display: grid;
    gap: .2rem;
}

.notice,
.job-state {
    padding: .8rem 1rem;
    border-radius: .8rem;
    background: rgb(33 150 243 / 12%);
}

.notice.success {
    background: var(--very-light-gray-green);
}

.notice.error {
    color: var(--medium-red-color);
    background: var(--red-opacity);
}

code {
    font-family: var(--mono-font);
}

@media (max-width: 720px) {
    .devices-page {
        width: min(100% - 1rem, 1120px);
        padding-top: 1rem;
    }

    .hero,
    .section-heading {
        align-items: flex-start;
    }

    .management-grid {
        grid-template-columns: 1fr;
    }

    .hero button {
        padding-inline: .75rem;
    }
}
